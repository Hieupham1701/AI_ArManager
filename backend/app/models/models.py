"""
Placeholder module for future SQLAlchemy / database models.

During the mock phase all data lives in-memory inside the service layer.
Once PostgreSQL is wired in, this module will host the ORM definitions
without affecting the Pydantic schemas in app/schemas.py.
"""
